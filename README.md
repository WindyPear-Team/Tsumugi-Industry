# Tsumugi Industry

Go application with an embedded Vite + React + shadcn/ui frontend.

## Development

```powershell
cd web
yarn install
yarn dev
```

## Build and run

```powershell
cd web
yarn build
cd ..
go run .
```

The Go server serves the embedded frontend at `http://localhost:8080`.
