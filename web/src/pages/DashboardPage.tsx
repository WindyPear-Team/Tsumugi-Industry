import { useEffect, useState } from "react"
import { AlertTriangle, ArrowUpRight, CheckCircle2, Database, LockKeyhole, Users, Wifi } from "lucide-react"
import { api, type Summary } from "@/lib/api"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export function DashboardPage() { const [summary, setSummary] = useState<Summary>({ users: 0, roles: 0, devices_online: 0, alerts: 0 }); useEffect(() => { api<Summary>("/api/dashboard/summary").then(setSummary).catch(() => undefined) }, []); const cards = [{ label: "在线设备", value: summary.devices_online, note: "实时连接", icon: Wifi }, { label: "活动告警", value: summary.alerts, note: "需要关注", icon: AlertTriangle }, { label: "系统用户", value: summary.users, note: "已授权账号", icon: Users }, { label: "角色策略", value: summary.roles, note: "权限集合", icon: LockKeyhole }]; return <><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{cards.map(({ label, value, note, icon: Icon }) => <Card key={label} className="border-border/70 shadow-none"><CardContent className="flex items-start justify-between p-5"><div><p className="text-sm text-muted-foreground">{label}</p><p className="mt-3 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-2 text-xs text-muted-foreground">{note}</p></div><div className="rounded-lg bg-muted p-2.5"><Icon className="size-4" /></div></CardContent></Card>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><Card className="border-border/70 shadow-none"><CardHeader><div className="flex items-center justify-between"><div><CardTitle>产线状态</CardTitle><CardDescription className="mt-1">核心生产单元运行概览</CardDescription></div><Button variant="outline" size="sm">查看详情 <ArrowUpRight /></Button></div></CardHeader><CardContent className="space-y-4">{["装配线 A-01", "包装线 B-02", "质检单元 Q-03"].map((name, index) => <div key={name} className="flex items-center gap-4 rounded-xl border border-border/60 p-4"><div className="size-2 rounded-full bg-foreground" /><div className="min-w-0 flex-1"><p className="font-medium">{name}</p><p className="text-xs text-muted-foreground">最后上报 {index + 1} 分钟前 · 运行负载 {62 + index * 8}%</p></div><span className="text-xs font-medium text-muted-foreground">运行中</span></div>)}</CardContent></Card><Card className="border-border/70 shadow-none"><CardHeader><CardTitle>系统活动</CardTitle><CardDescription className="mt-1">最近的关键事件</CardDescription></CardHeader><CardContent className="space-y-5">{[["系统初始化完成", "admin 创建了系统", CheckCircle2], ["权限策略已加载", "8 项细粒度权限", LockKeyhole], ["数据服务在线", "SQLite / GORM", Database]].map(([title, detail, Icon]) => <div className="flex gap-3" key={title as string}><div className="rounded-full border border-border p-2"><Icon className="size-3.5" /></div><div><p className="text-sm font-medium">{title as string}</p><p className="mt-1 text-xs text-muted-foreground">{detail as string}</p></div></div>)}</CardContent></Card></div></> }
