import { useEffect, useState } from "react"
import { AlertTriangle, ArrowUpRight, CheckCircle2, ClipboardList, Database, LockKeyhole, Users, Wifi } from "lucide-react"
import { api, type Summary } from "@/lib/api"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export function DashboardPage() { const [summary, setSummary] = useState<Summary>({ users: 0, roles: 0, devices_online: 0, alerts: 0, work_orders_running: 0, work_orders_waiting: 0 }); useEffect(() => { api<Summary>("/api/dashboard/summary").then(setSummary).catch(() => undefined) }, []); const cards = [{ label: "在线设备", value: summary.devices_online, note: "实时连接", icon: Wifi }, { label: "生产中工单", value: summary.work_orders_running, note: `${summary.work_orders_waiting} 个待执行/暂停`, icon: ClipboardList }, { label: "活动告警", value: summary.alerts, note: "需要关注", icon: AlertTriangle }, { label: "系统用户", value: summary.users, note: "已授权账号", icon: Users }, { label: "角色策略", value: summary.roles, note: "权限集合", icon: LockKeyhole }]; return <><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{cards.map(({ label, value, note, icon: Icon }) => <Card key={label} className="border-border/70 shadow-none"><CardContent className="flex items-start justify-between p-5"><div><p className="text-sm text-muted-foreground">{label}</p><p className="mt-3 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-2 text-xs text-muted-foreground">{note}</p></div><div className="rounded-lg bg-muted p-2.5"><Icon className="size-4" /></div></CardContent></Card>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><Card className="border-border/70 shadow-none"><CardHeader><div className="flex items-center justify-between"><div><CardTitle>生产执行</CardTitle><CardDescription className="mt-1">工单状态由现场流转接口实时维护</CardDescription></div><Button variant="outline" size="sm">查看工单 <ArrowUpRight /></Button></div></CardHeader><CardContent className="space-y-4"><div className="rounded-xl border border-border/60 p-4"><div className="flex items-center gap-3"><div className="size-2 rounded-full bg-foreground" /><p className="font-medium">当前生产执行闭环</p></div><p className="mt-2 text-sm text-muted-foreground">工单下达后按工序启动、暂停、恢复和报工，所有数量与异常原因都会进入生产事件和审计日志。</p></div><div className="grid gap-3 sm:grid-cols-2"><div className="rounded-lg bg-muted/50 p-3"><p className="text-xs text-muted-foreground">正在生产</p><p className="mt-1 text-xl font-semibold">{summary.work_orders_running}</p></div><div className="rounded-lg bg-muted/50 p-3"><p className="text-xs text-muted-foreground">待处理工单</p><p className="mt-1 text-xl font-semibold">{summary.work_orders_waiting}</p></div></div></CardContent></Card><Card className="border-border/70 shadow-none"><CardHeader><CardTitle>系统活动</CardTitle><CardDescription className="mt-1">最近的关键事件</CardDescription></CardHeader><CardContent className="space-y-5">{[["系统初始化完成", "admin 创建了系统", CheckCircle2], ["权限策略已加载", "生产执行权限已启用", LockKeyhole], ["数据服务在线", "SQLite / GORM", Database]].map(([title, detail, Icon]) => <div className="flex gap-3" key={title as string}><div className="rounded-full border border-border p-2"><Icon className="size-3.5" /></div><div><p className="text-sm font-medium">{title as string}</p><p className="mt-1 text-xs text-muted-foreground">{detail as string}</p></div></div>)}</CardContent></Card></div></> }
